/*
package com.leetcode.algorithmen.binarySearch;

import java.util.ArrayList;
import java.util.Arrays;

public class MagneticForceBetweenTwoBalls {
    public int maxDistance(int[] position, int m) {
        if (m > position.length) {
            throw new IllegalArgumentException();
        }
        int force = 0;
        Arrays.sort(position);

        int left = 0;
        int right = position.length - 1;

        while (left <= right) {
            int mid = (left + right) / 2;


        }
    }

    public static void main(String[] args) {
        MagneticForceBetweenTwoBalls magneticForceBetweenTwoBalls = new MagneticForceBetweenTwoBalls();
        int[] position = {1, 2, 3, 4, 7};
        magneticForceBetweenTwoBalls.maxDistance(position, 3);
    }
}
*/
